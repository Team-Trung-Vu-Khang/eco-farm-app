export interface GroupCropDocument {
  name: string;
  url: string;
}

export interface GroupCrop {
  id: number;
  code: string;
  name: string;
  scientificName: string;
  category: string;
  description: string;
  createdAt: string;
}
