/**
 * Thin wrapper — delegates to the shared GeographicalSelector.
 * Defaults to single-select (multiSelect={false}).
 */
export {
  default,
  type GeographicalSelection,
  type RegionOption,
} from "@/shared/components/GeographicalSelector/index";
